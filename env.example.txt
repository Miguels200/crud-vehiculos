# Copia este archivo como .env y completa con tus datos

# MongoDB Connection String
# Para MongoDB Atlas (recomendado si no tienes MongoDB instalado):
# Obtén tu connection string desde: https://cloud.mongodb.com
# Formato: mongodb+srv://usuario:password@cluster.mongodb.net/crud-vehiculos?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/crud-vehiculos

# Puerto del servidor (opcional)
PORT=3000

